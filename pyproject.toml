[project]
name = "agent-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SohaKhare", email = "sohaajaykhare2006@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "google-genai>=2.24.0",
    "jupyter>=1.1.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
agent-learning = "agent_learning:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
